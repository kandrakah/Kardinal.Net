namespace Kardinal.Net.Web.Auth
{
    public static class AuthConstants
    {
        public static class Cookies
        {
            public const string DefaultAuthenticationScheme = "kapsvr";

            public const string ExternalAuthenticationScheme = $"{DefaultAuthenticationScheme}.external";
        }
    }
}
